id = "herdr-remote.relay"
name = "Herdr Remote Relay"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Push agent events to the herdr-remote relay over authenticated HTTP"
platforms = ["macos", "linux"]

[[events]]
on = "pane.agent_status_changed"
command = ["python3", "on_event.py"]
